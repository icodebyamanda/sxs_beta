import  { createContext } from 'react';

// In conjunction with useAuth: https://usehooks.com/useAuth/
// Give the ability to create a context

const authContext = createContext();

export default authContext;